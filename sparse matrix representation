#include <stdio.h>

void sparse(int a[][10], int m, int n) {
int k = 1;
int b[100][3]; // Sparse matrix representation
b[0][0] = m;
b[0][1] = n;

for (int i = 0; i < m; i++) {  
    for (int j = 0; j < n; j++) {  
        if (a[i][j] != 0) {  
            b[k][0] = i;  
            b[k][1] = j;  
            b[k][2] = a[i][j];  
            k++;  
        }  
    }  
}  

b[0][2] = k - 1; // Number of non-zero elements  

printf("The sparse matrix:\n");  
for (int i = 0; i < k; i++) {  
    printf("%d\t%d\t%d\n", b[i][0], b[i][1], b[i][2]);  
}

 
int main() {
int m, n,i,j;
int a[10][10];

printf("Enter row & column of matrix: ");  
scanf("%d%d", &m, &n);  

printf("Enter elements:\n");  
for (i = 0; i < m; i++) {  
    for ( j = 0; j < n; j++) {  
        scanf("%d", &a[i][j]);  
    }  
}  

sparse(a, m, n);  
}

Algorithm

Algorithm for main function

Step 1: Start
Step 2: Read the number of rows m and columns n.
Step 3: Read the elements of matrix a[m][n].
Step 4: Call function sparse(a, m, n).
Step 5: Stop

Algorithm for sparse function

Step 1: Start
Step 2: Initialize k = 1.
Step 3: Set b[0][0] = m, b[0][1] = n.
Step 4: Repeat Steps 5–7 for each element of the matrix with i = 0 to m-1 and j = 0 to n-1.
Step 5: If a[i][j] != 0, then
    a) Set b[k][0] = i.
    b) Set b[k][1] = j.
    c) Set b[k][2] = a[i][j].
    d) Increment k = k + 1.
    Go to Step 7.
Step 6: Otherwise, go to Step 7.
Step 7: Continue until all elements are checked.
Step 8: Set b[0][2] = k - 1.
Step 9: Display the sparse matrix b.
Step 10: Stop
}
