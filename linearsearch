
#include <stdio.h>
int linearsearch(int a[], int n, int target)
{
    for (int i=0;i<n;i++)
    {
        if(a[i]==target)
        {
            return i;
        }
    }
   return -1; 
}
void main()
{
    int n,p,a[10],target;
    printf("enter the number of elements limit :");
    scanf("%d",&n);
    printf("enter the elements:");
    for (int i=0;i<n;i++)
    scanf("%d",&a[i]);
    printf("enter the target:");
    scanf("%d",&target);
    p=linearsearch(a,n,target);
    if(p!=-1)
    printf("element is present at position %d \n",p+1);
    else
    printf("elemet is not present");
}


✨ Algorithm for main() function

Step 1: Start
Step 2: Read the limit n.
Step 3: Read n elements into array a[].
Step 4: Read the target element target.
Step 5: Call linearsearch(a, n, target) and store the result in p.
Step 6: If p != -1, then
    Display “Element is present at position p+1”.
Else
    Display “Element is not present”.
Step 7: Stop

✨ Algorithm for linearsearch() function

Step 1: Start
Step 2: Input array a[], number of elements n, and target.
Step 3: Repeat for each index i from 0 to n-1:
    If a[i] == target, then
      Return index i.
Step 4: If no element matches, return -1.
Step 5: Stop


